
# TokenPod+ Local Development Configuration
# Rename this file to .env.local and populate with real values.

# The deployment URL of your Google Apps Script Ledger.
# Found in GAS Editor > Deploy > Manage Deployments.
# Use the VITE_ prefix for local testing with the Vite proxy.
VITE_GAS_SCRIPT_URL=https://script.google.com/macros/s/REPLACE_WITH_YOUR_ID/exec

# NOTE: For Vercel deployments, add GAS_SCRIPT_URL (without VITE_ prefix) 
# to your Project Settings > Environment Variables.
